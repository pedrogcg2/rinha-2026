package pb;

option go_package = ".";

service GetScoreService {
  rpc GetScore (VectorRequest) returns (ScoreResponse);
}

message VectorRequest {
  repeated float values = 1;
}

message ScoreResponse {
  repeated float score = 1;
}
